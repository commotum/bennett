import Bennett.Turing.Quadruple.Split
import Bennett.Turing.Source.Inverse

/-!
# Reversible quadruple public API

Stable re-export for typed heterogeneous tape actions, formal inverse rules,
semantic overlap criteria, finite reversible rule tables, constructive source
quintuple inverses, and fresh-state two-rule splitting.  Diagnostics remain in
`Bennett.Turing.Quadruple.Audit`.
-/
