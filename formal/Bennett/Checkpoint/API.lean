import Bennett.Checkpoint.Plan

/-!
# Checkpointing public API

Stable re-export for abstract reversible checkpoint stages and chains,
recorded implementations of irreversible source segments, constructive integer
segment plans, exact discrete storage and dump-I/O time costs, and the
separately named continuous relaxation.  Executable diagnostics remain in
`Bennett.Checkpoint.Audit`.
-/
